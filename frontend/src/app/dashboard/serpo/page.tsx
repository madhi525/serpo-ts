"use client"

import { verifikasiToken } from "../../../../utils/verifikasiToken";

const Fleet = () => {
    return (
      <div>
        <p>fleet</p>
      </div>
    );
  }

export default verifikasiToken(Fleet, ['serpo']);
